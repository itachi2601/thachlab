"use client";

import { useCallback, useEffect, useState } from "react";
import { CalendarCheck, CheckCircle2, Clock3 } from "lucide-react";
import { fetchMyAttendance, submitAttendanceCode } from "@/services/course-attendance";

const labels:Record<string,string>={present:"Có mặt",late:"Đi muộn",excused:"Vắng có phép",absent:"Vắng không phép"};
export default function StudentAttendancePanel({courseId,studentId}:{courseId:number;studentId:string}){const[code,setCode]=useState("");const[rows,setRows]=useState<Awaited<ReturnType<typeof fetchMyAttendance>>>([]);const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);const load=useCallback(()=>fetchMyAttendance(courseId,studentId).then(setRows).catch(()=>undefined),[courseId,studentId]);useEffect(()=>{void load()},[load]);const attended=rows.filter(row=>row.status==="present"||row.status==="late").length;const rate=rows.length?Math.round(attended/rows.length*100):0;async function submit(){setBusy(true);try{const result=await submitAttendanceCode(courseId,code);setMessage(`${result.status==="late"?"Điểm danh muộn":"Điểm danh thành công"}: ${result.session_title}`);setCode("");await load()}catch(cause){setMessage(cause instanceof Error?cause.message:"Không thể điểm danh.")}finally{setBusy(false)}}return <section className="mt-5 rounded-2xl border border-cyan-400/15 bg-cyan-500/5 p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2"><CalendarCheck size={18} className="text-cyan-300"/><strong className="text-white">Điểm danh offline</strong></div><p className="mt-1 text-xs text-slate-400">Đã tham dự {attended}/{rows.length} buổi · {rate}%</p></div><div className="flex gap-2"><input inputMode="numeric" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,""))} placeholder="Mã 6 số" className="w-32 rounded-xl border border-white/10 bg-[#080d1d] px-3 py-2 font-mono text-sm tracking-wider text-white"/><button disabled={busy||code.length!==6} onClick={submit} className="rounded-xl bg-cyan-600 px-4 py-2 text-xs font-bold text-white disabled:opacity-40">Xác nhận</button></div></div>{message&&<p className="mt-3 rounded-lg bg-black/20 p-2 text-xs text-cyan-100">{message}</p>}<div className="mt-3 grid gap-2 sm:grid-cols-2">{rows.slice(0,4).map(row=>{const session=Array.isArray(row.attendance_sessions)?row.attendance_sessions[0]:row.attendance_sessions;return <div key={row.session_id} className="flex items-center justify-between rounded-xl border border-white/5 bg-black/15 p-3"><div><strong className="block text-xs text-white">{session?.title??"Buổi học CNC"}</strong><small className="text-slate-500">{session?.session_date?new Date(`${session.session_date}T00:00:00`).toLocaleDateString("vi-VN"):""}</small></div><span className={`flex items-center gap-1 text-xs font-bold ${row.status==="present"?"text-emerald-300":row.status==="late"?"text-amber-300":row.status==="excused"?"text-blue-300":"text-red-300"}`}>{row.status==="present"?<CheckCircle2 size={14}/>:<Clock3 size={14}/>} {labels[row.status]}</span></div>})}</div></section>}
